// console.log(a); // undefined
// var a = 10;

var a;
console.log(a); // undefined
a = 10;

// c++ ke pass garbage value by default store  hota hai
// java mein 0 store hota hai
// js mein undefined store hota hai
// java and js are slower than c++



// js file run in 2 phase
// 1. memory creation phase
// 2. code execution phase

// js --> just in compiler --> ye compile nahi hoti.(no complile time error only runtime error).
